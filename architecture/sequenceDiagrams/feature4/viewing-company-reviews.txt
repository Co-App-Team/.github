title Viewing a comapny's reviews on Rate My Co-op Screen

actor User
participant Company list
participant Individual Company Modal
participant API
participant Service
participant MongoDB

Company list->User:Shows user the \ncurrent company list (all companies)
User->Company list: Clicks on a single company
Individual Company Modal->User: Shows a modal with company information
Individual Company Modal-->API: GET /api/companies/{companyId}/reviews \nfor the company the user clicked
API-->Service: Send request details to backend.\nReviewService processes request.
Service-->MongoDB: send details to database
MongoDB-->Service: send success response\nwith company reviews
Service-->API: send success response \nwith company reviews
Individual Company Modal->User: Displays a loading spinner
API-->Individual Company Modal: GET /api/companies/{companyId}/reviews \nreturns the first page of reviews
Individual Company Modal->User: Displays all the reviews that were returned
User->Individual Company Modal: Clicks "next page"
Individual Company Modal-->API: GET /api/companies/{companyId}/reviews \nfor the company the user clicked
Individual Company Modal->User: Displays a loading spinner
API-->Individual Company Modal: GET /api/companies/{companyId}/reviews \nreturns the next page of reviews
Individual Company Modal->User: Displays all the reviews that were returned
User->Individual Company Modal: Clicks "previous page"
Individual Company Modal-->API: GET /api/companies/{companyId}/reviews \nfor the company the user clicked
Individual Company Modal->User: Displays a loading spinner
API-->Individual Company Modal: GET /api/companies/{companyId}/reviews \nreturns the previous page of reviews
Individual Company Modal->User: Displays all the reviews that were returned