title Login

actor User
participant Log-in page
participant API
participant Auth Service
database User Repository


User->Log-in page:Enter username
User->Log-in page:Enter Password
User->Log-in page:Click "Log in"

Log-in page->User: Show loading spinner

Log-in page-->API: POST /api/auth/login

API --> Auth Service: Email\nPassword

Auth Service --> User Repository: Email

Auth Service <-- User Repository: User Model

Auth Service --> Auth Service: Check password


Log-in page<--Auth Service:/api/auth/login OK (Set auth cookie)

Log-in page->User: Redirect to app