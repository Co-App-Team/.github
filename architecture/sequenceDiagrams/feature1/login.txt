title Login

actor User
participant Log-in page
participant API

User->Log-in page:Enter username
User->Log-in page:Enter Password
User->Log-in page:Click "Log in"

Log-in page->User: Show loading spinner

Log-in page-->API: POST /api/login
Log-in page<--API:/api/auth/login OK (Set auth cookie)

Log-in page->User: Redirect to app